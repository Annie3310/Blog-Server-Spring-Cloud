mybatis:
  configuration:
    map-underscore-to-camel-case: true
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package: top.catcatc.mapper.mapper
server:
  port: 6006
spring:
  application:
    name: blog-server